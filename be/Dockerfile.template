# Node.js 기반 백엔드 서비스 Dockerfile 템플릿
# 사용법: SERVICE_NAME을 실제 서비스 이름으로 변경하여 사용

FROM node:18-alpine

# 작업 디렉토리 설정
WORKDIR /app

# package.json과 package-lock.json 복사
COPY package*.json ./

# 의존성 설치
RUN npm ci --only=production

# 소스 코드 복사
COPY . .

# Babel 빌드 (dist 폴더 생성)
RUN npm run build

# 환경 변수 설정
ENV NODE_ENV=production

# 포트 노출 (서비스별로 변경 필요)
EXPOSE 3000

# 애플리케이션 실행
CMD ["node", "dist/app.js"]

