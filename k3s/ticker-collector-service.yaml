apiVersion: v1
kind: Service
metadata:
  name: ticker-collector
  namespace: bonanza-index
  labels:
    app: ticker-collector
spec:
  type: ClusterIP
  ports:
  - port: 6002
    targetPort: 6002
    protocol: TCP
    name: http
  selector:
    app: ticker-collector

