apiVersion: v1
kind: Service
metadata:
  name: index-calc-fe-service
  namespace: bonanza-index
  labels:
    app: index-calc-fe
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30076
    protocol: TCP
    name: http
  selector:
    app: index-calc-fe

