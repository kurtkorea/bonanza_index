apiVersion: v1
kind: Service
metadata:
  name: index-calc-fe-service
  namespace: bonanza-index
  labels:
    app: index-calc-fe
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: index-calc-fe

